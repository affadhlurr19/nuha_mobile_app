import 'package:flutter/material.dart';

class FavoriteList extends StatelessWidget {
  static const String favoriteTitle = 'My Favorite';
  const FavoriteList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}